{
  "name": "blackjack-trainer",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd client && npm install",
    "build": "cd client && npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}
